# ESP32-C6 WiFi Configuration via LittleFS (JSON)

โปรเจกต์ต้นแบบสำหรับการเชื่อมต่อ WiFi บน **ESP32-C6-WROOM-N16** โดยใช้การอ่านค่าจากไฟล์คอนฟิกูเรชัน (`config.json`) ที่จัดเก็บในหน่วยความจำ **LittleFS** ระบบนี้ช่วยให้สามารถเปลี่ยน SSID, Password หรือ Port ได้อย่างสะดวกโดยการอัปโหลดไฟล์ใหม่ แทนการแก้ไขโค้ดและคอมไพล์ใหม่ทุกครั้ง



## 🛠 การตั้งค่าใน Arduino IDE 2.x

เพื่อให้ซอฟต์แวร์ทำงานร่วมกับฮาร์ดแวร์ **ESP32-C6 (16MB)** ได้อย่างสมบูรณ์ ต้องตั้งค่าในเมนู **Tools** ดังนี้:

| รายการเมนู (Menu Item) | การตั้งค่า (Setting) | คำอธิบาย |
|-------------------|------------------|-------|
| **Board** | ESP32C6 Dev Module | เลือกให้ตรงกับสถาปัตยกรรม RISC-V ของ C6 |
| **USB CDC On Boot** | **Enabled** | **จำเป็น:** เพื่อให้คำสั่ง `Serial.print` แสดงผลผ่าน USB |
| **Flash Size** | 16MB (128Mb) | ให้ตรงกับรุ่น N16 |
| **Partition Scheme** | **Custom** | เพื่อให้ระบบเรียกใช้ไฟล์ `partitions.csv` ในโฟลเดอร์โปรเจกต์ |

---

## 💾 การติดตั้ง Plugin สำหรับอัปโหลด LittleFS

เนื่องจาก Arduino IDE 2.x ไม่มีเมนู "Sketch Data Upload" มาให้ในตัว ต้องติดตั้งเครื่องมือเสริมดังนี้:

1. **ดาวน์โหลดเครื่องมือ:** ไปที่ [arduino-littlefs-upload](https://github.com/earlephilhower/arduino-littlefs-upload?tab=readme-ov-file)
2. **การติดตั้ง:** นำไฟล์ไปวางในโฟลเดอร์ Plugin ของ Arduino IDE 
   - (โดยปกติอยู่ที่ `C:\Users\<YourName>\.arduinoIDE\plugins`)
3. **การใช้งาน:**
   - **ปิด Serial Monitor ก่อนทุกครั้ง** (เพื่อไม่ให้ Port ไม่ว่าง)
   - กดปุ่ม `Ctrl + Shift + P` (Command Palette)
   - พิมพ์คำว่า **Upload LittleFS to Pico/ESP32** แล้วกด Enter



---

## ⚙️ การตั้งค่าไฟล์ config.json

สร้างโฟลเดอร์ชื่อ `data` ไว้ในโฟลเดอร์เดียวกับโปรเจกต์ และสร้างไฟล์ชื่อ `config.json` ไว้ข้างในโดยมีโครงสร้างดังนี้:

```json
{
  "wifi_ssid": "ชื่อ_WiFi_ของคุณ",
  "wifi_password": "รหัสผ่าน_ของคุณ",
  "port": 80
}
